'using strict';

class UIUtils{
    constructor(){}

    generateHtmlFor(params){
        return f[params.type];

        const f = {
            noteLabel: noteLabel
        };

        function noteLabel(title, date) {
            
        }
        //noteLabel
    }
}